spring:
  application:
    name: project

  datasource:
    url: jdbc:h2:mem:orderdb
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

  h2:
    console:
      enabled: true

server:
  port: 8081